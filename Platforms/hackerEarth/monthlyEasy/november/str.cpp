#include <iostream>
#include <string.h>
using namespace std;

int main()
{
    char str[100];
    int len;
    cin >> str;
    len = strlen(str);

    for (int i = 0; i < len; i++)
    {
        if (str1[i] > str2[i])
        {
        }
    }
}