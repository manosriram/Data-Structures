#include <iostream>
#include "basicActions.h"
using namespace std;

bool findPalindrome() {

}


int main() {
  Node *head = NULL;

  insertAtEnd(head,1);
  insertAtEnd(head,2);
  insertAtEnd(head,3);
  insertAtEnd(head,4);
  insertAtEnd(head,5);

  cout << findMiddle(head) << endl;
}
